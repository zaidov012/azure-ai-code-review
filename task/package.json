{
  "name": "ai-code-review-task",
  "version": "2.0.0",
  "description": "Azure Pipelines task for AI-powered code review",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "package": "npm run build && node scripts/package-task.js",
    "clean": "rimraf dist"
  },
  "keywords": [
    "azure-pipelines",
    "code-review",
    "ai",
    "pull-request"
  ],
  "author": "Simbrella",
  "license": "MIT",
  "dependencies": {
    "azure-pipelines-task-lib": "^4.6.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.10.0",
    "@types/q": "^1.5.8",
    "jest": "^29.7.0",
    "rimraf": "^5.0.5",
    "ts-jest": "^29.1.1",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
